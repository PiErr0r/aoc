


// const ord = (c) => (c.charCodeAt(0));
// const chr = (n) => (String.fromCharCode(n));

module.exports = {
	...require('./logging'),
	...require('./range'),
	...require('./string'),
	...require('./math'),
	...require('./set'),
}
